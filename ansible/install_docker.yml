- hosts: task-tracker-server
  become: yes

  tasks:

    - name: update apt
      apt:
        update_cache: yes

    - name: install docker
      apt:
        name: docker.io
        state: present

    - name: start docker
      service:
        name: docker
        state: started
        enabled: yes